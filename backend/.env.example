# Backend server port
PORT=5000

# MongoDB connection
MONGO_URI=mongodb://localhost:27017/mydatabase

# Frontend origin allowed to call the backend
CORS_ORIGIN=http://localhost:3000

# Simple file-backed auth database
AUTH_DB_FILE=data/users.json
AUTH_DEFAULT_USERNAME=operator
AUTH_DEFAULT_PASSWORD=GodsEye2026!
AUTH_DEFAULT_DISPLAY_NAME=Primary Operator
AUTH_DEFAULT_ROLE=administrator
AUTH_SESSION_TTL_DAYS=14
